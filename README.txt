to run the program you need:
1. node

open the code on your text editor and run the following commands on command line
1. npm i        // this is to install dependencies
2. npm run dev  // run the program after installation

open the app on your browser at localhost:3000